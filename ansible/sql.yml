- name: Configure MySQL
  hosts: all
  become: yes

  tasks:
    - name: Disable MySQL module
      ansible.builtin.shell: dnf module disable mysql -y
      args:
        creates: /etc/yum.repos.d/mysql.repo

    - name: Copy MySQL repository file
      ansible.builtin.copy:
        src: mysql.repo
        dest: /etc/yum.repos.d/mysql.repo
        owner: root
        group: root
        mode: '0644'

    - name: Install MySQL Community Server
      ansible.builtin.package:
        name: mysql-community-server
        state: present

    - name: Enable and start MySQL service
      ansible.builtin.systemd:
        name: mysqld
        enabled: yes
        state: started

    - name: Set MySQL root password
      ansible.builtin.shell: |
        mysql -uroot -e "ALTER USER 'root'@'localhost' IDENTIFIED BY 'ExpenseApp@1';"
      args:
        creates: /var/lib/mysql/mysql.sock
      
    - name: Verify MySQL connection
      ansible.builtin.shell: |
        mysql -uroot -pExpenseApp@1 -e "SELECT 1;"
      register: mysql_test
      changed_when: false